---
title: Mercury
description: Fast, drop-in CQL evaluation for FHIR quality measures. Community runs free self-hosted Core; AWS Marketplace will offer paid Core by container and AMI.
template: splash
hero:
  tagline: A CQL evaluation engine for FHIR quality measures, built in Rust. It speaks the same FHIR CQF API as the reference stack, so it drops into the pipeline you already run — only far faster. Community runs free self-hosted Core under closed-source terms; AWS Marketplace will offer paid Core by container and AMI.
  image:
    file: ../../assets/logo.png
  actions:
    - text: Quickstart
      link: /quickstart
      icon: right-arrow
      variant: primary
    - text: What's in Core
      link: '#whats-in-the-community-edition'
      variant: minimal
---

import { Card, CardGrid, LinkCard, Aside, Badge } from '@astrojs/starlight/components';
import { withBase } from '../../utils/base';

:::note[Limited release]
Mercury is currently available to select partners. There's no public download yet — [register your interest](https://getcql.com/#request-access) to get Community Edition access. This site documents the self-hosted Core experience.
:::

## Why Mercury

<CardGrid>
  <Card title="Fast" icon="rocket">
    Rebuilt in Rust on Actix. In early side-by-side benchmarks, one to two orders of magnitude faster than CQF Ruler, workload depending. <Badge text="preliminary" variant="caution" />
  </Card>
  <Card title="Drop-in" icon="random">
    The same FHIR CQF API and CLI contract as the reference stack. Point your existing tools at a new URL — no rewrites.
  </Card>
  <Card title="Simple to operate" icon="puzzle">
    One binary, one container, one AMI path, no JVM. Fewer services to deploy, monitor, and keep alive. Durable local persistence.
  </Card>
  <Card title="Validated" icon="open-book">
    Checked against the HL7 CQL 1.5.2 language test suite. See <a href={withBase('/support/conformance')}>Conformance &amp; correctness</a>.
  </Card>
</CardGrid>

## What's in the Community Edition

The Community Edition runs Mercury Core for free in your own infrastructure under closed-source terms. Interoperability, observability, and other add-on packs are future add-ons and are not included.

- The full Mercury CQL evaluation engine.
- **FHIR CQF REST API:** `/metadata`, `Library` and `Measure` CRUD, and `$evaluate` — plus `$data-requirements` and `$package` stubs for harness compatibility.
- **A broader REST surface** under `/api` and `/fhir`: `/health` and `/ready` checks, `/execute`, `/translate`, `/query`, library and value-set management, and an `/openapi` spec.
- **Direct CQL compilation,** with ELM XML accepted as an alternative input.
- **Durable local persistence** — loaded libraries, measures, and ingested data survive restarts.
- **CLI tools:** `serve`, `run`, `edition`, and `lang-tests` for conformance.
- **Structured logging** (pretty or JSON) with configurable levels.
- **Single binary and Docker image;** composes with existing MCT and facility FHIR servers.

<Aside type="tip" title="AWS Marketplace">
Mercury is being prepared for paid AWS Marketplace Core offerings as both a container and an AMI. The planned listing shape is contract pricing for 1-month and 12-month terms only. There is no hourly metering path, and add-on packs are not included.
</Aside>

## What Mercury isn't

- **No built-in authentication or RBAC.** Run Mercury on a private network behind your own gateway, service mesh, load balancer, or API gateway.
- **No HIPAA attestation, BAA, or SOC 2 report.** Mercury is software you run in your own infrastructure, under your own compliance program and controls.
- **Not a managed service.** You deploy, operate, monitor, patch, and back it up.
- **Core only.** Integration, observability, HA, security, and data-quality packs are future add-ons, not included in Community or the AWS Marketplace Core listing.
- **Closed source.** Community Edition is free to run under closed-source terms.
- **x86_64 only today.**

## Start here

<CardGrid>
  <LinkCard title="Quickstart" description="From zero to a first evaluation you can compare against your reference engine." href={withBase('/quickstart')} />
  <LinkCard title="Running the containers" description="The drop-in Measure Calculator stack, and running the server directly." href={withBase('/containers')} />
  <LinkCard title="Loading data: preload vs inline" description="Two ways to get patient data into an evaluation — and when to use each." href={withBase('/loading-data')} />
  <LinkCard title="Mounting your data drive" description="Keep libraries, measures, and data across restarts with a mounted volume." href={withBase('/data-persistence')} />
  <LinkCard title="FAQ & security" description="Security, entitlement, support, and compliance boundaries." href={withBase('/support/faq')} />
</CardGrid>

## Beyond Core

Mercury Core covers evaluation end to end. Optional **add-on packs** will layer extra capability on top of the same engine — they're **additive and independent, not tiers**: Connect (integration & bulk data), Insight (observability), HA (reliability), Security (identity & governance), and Trust / Quality (data-quality scoring). They are future add-ons and are not included in the Community or Path A AWS Marketplace Core offering.

## Getting help

Community Edition support is **self-service**: this documentation site, docs repo issue forms where available, and [getcql.com](https://getcql.com/#request-access) for access or private follow-up. Guaranteed response times and hands-on support are planned as a paid offering.

Already evaluating? <a href={withBase('/feedback')}>Send evaluation feedback</a> — it goes straight to the Mercury team and shapes what ships next.
